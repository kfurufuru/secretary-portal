# 引き継ぎ: knowledge-graph.html UI改善（取説なしで使えるUIへ）

作成: 2026-04-25

## 背景

`knowledge/knowledge-graph.html` を大幅改善済み。ただし「活用方法が不明」という課題が残っている。
**方針: 説明ページは作らず、UI自体を自己説明型にする。**

## 対象ファイル

`C:\Users\kfuru\.secretary\knowledge\knowledge-graph.html`
→ `http://localhost:8092/knowledge/knowledge-graph.html` で確認可能

## 現在の状態

- 中心ノード「古舘ナレッジベース」＋カテゴリハブ構造
- 右側詳細パネル（クリックで開く）
- 検索・カテゴリ・ステータスフィルター
- ノード: 16、RAW_EDGES: 33本
- published: 14、review: 2、draft: 0

## 実装してほしい改善内容

### 1. 初回オーバーレイ（最優先）
- グラフ上に薄い半透明オーバーレイを表示（初回のみ）
- 中央に3ステップのガイド:
  - 🖱 **クリック** → 右パネルで詳細・関連ノードを確認
  - 🔍 **検索・フィルター** → カテゴリや状態で絞り込み
  - ✋ **ドラッグ／スクロール** → 自由に探索
- 「わかった、始める」ボタンで閉じる
- localStorage に既読フラグを保存（2回目以降は表示しない）

### 2. 右パネルの初期状態を操作ガイドに
- ノード未選択時の右パネル（現在は `display: none`）を常時表示に変更
- 初期コンテンツとして:
  ```
  ノードをクリックして詳細を見る
  
  💡 ヒント
  • ★ = published（読み返し推奨）
  • ◆ = review（理解浅め・要フェインマン）
  • サイズ大 = 理解度スコア高
  • エッジのラベル = 知識の関係性
  ```

### 3. フィルターボタンにツールチップ
- 各カテゴリボタンにホバーで説明:
  - AI活用: 「プロンプト設計・ツール選定・自動化」
  - Claude Code: 「MCP・Skills・Hooks・パイプライン」
  - 設備技術: 「製造現場AIファースト・IoT・計装」
  - 業務改善: 「フレームワーク・可観測性」
  - ビジネススキル: 「思考法・マネジメント」

### 4. ヘッダーに「？」ボタン
- クリックで操作ガイドオーバーレイを再表示（上記1と同じ内容）

## 実装注意点

- 1ファイル完結（HTML + style + script）を維持
- vis-network@9.1.9 使用中
- デザイン: Slate Frost テーマ（--bg: #0f172a、--accent: #38bdf8）
- 実装前に必ず `Read` してから `Edit`
- 完了後 `node -c` で JS構文チェック
- URL: `http://localhost:8092/knowledge/knowledge-graph.html`

## 完了条件

グラフを初めて開いたユーザーが「何もわからない」状態でも、
オーバーレイとパネルだけで操作方法が理解できること。
